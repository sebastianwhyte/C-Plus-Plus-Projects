#include <iostream>
#include "Game.h"

int main() {
    // Make an object (which is called "game") from the GAME CLASS
    Game game;
    game.run();     // Call for run METHOD by using game OBJECT
    return 0;
}
